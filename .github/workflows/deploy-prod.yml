name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: 'echo "TODO: replace with your real build steps"'

      - name: Deploy to prod
        run: 'echo "TODO: replace with your real deploy to prod steps"'

      - name: Health check
        run: 'echo "TODO: replace with your real health check steps"'

      - name: Traffic shift
        run: 'echo "TODO: replace with your real traffic shift steps"'
